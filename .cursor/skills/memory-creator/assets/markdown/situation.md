# Situation (working memory)

Volatile. TTL drops **automatic inject**, not “never existed.” Do not put rules here. Do not commit this file if the project uses Git.

<!-- Default TTL is adapter-tunable (48h is a common arbitrary starting point). -->
