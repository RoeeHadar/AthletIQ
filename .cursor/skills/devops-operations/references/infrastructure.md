# Infrastructure reference

## Doc

`docs/09-devops/infrastructure.md` + root `docker-compose.yml`

## MVP services

`etl`, `database` (PostgreSQL), `api` (FastAPI). Local Compose is the supported topology until a cloud target is chosen.
