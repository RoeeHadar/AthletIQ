# Lead review — GitHub Actions remote green on 4a2f713

Status: Complete  
Date: 2026-08-14  
Reviewer role: Lead manager / independent engineering reviewer  
Scope: Publish-bar remote GitHub Actions evidence for OPS-001, CON-005, and NFR-003

## Decision

**APPROVE with notes.**

GitHub Actions run [31753742525](https://github.com/RoeeHadar/AthletIQ/actions/runs/31753742525) is valid remote evidence for the requested commit and CI path. The run completed with `success`, was triggered by `push` on `main`, and reports head SHA `4a2f713ed4cf58966358c6dd12b3ea77813d905a`. GitHub identifies that commit as **“Bind live NBA ingest to the no-key NBA Stats API.”**

The required follow-up is documentation-only: update the stale remote-CI verification wording in `docs/03-requirements/traceability.md` as specified below. This does not block approval of the run itself.

## Independent remote evidence

The evidence was queried from GitHub with `gh`; local pytest was not used as proof of remote CI.

- Workflow: `ci`
- Event / branch: `push` / `main`
- Run status / conclusion: `completed` / `success`
- Head SHA: `4a2f713ed4cf58966358c6dd12b3ea77813d905a`
- `lint`: success, started `23:25:12Z`, completed `23:25:46Z`
- `unit`: success, started `23:25:12Z`, completed `23:25:41Z`
- `integration`: success, started `23:25:48Z`, completed `23:26:30Z`
- `image`: success, started `23:26:32Z`, completed `23:27:49Z`

The simultaneous `lint` and `unit` starts, followed by `integration`, then `image`, agree with the declared workflow dependencies. The exact `ci.yml` retrieved from GitHub at the reviewed SHA declares:

- `lint` and `unit` with no interdependency;
- `integration.needs: [lint, unit]`;
- `image.needs: [integration]`.

This satisfies the OPS-001 four-stage topology and CON-005 GitHub Actions constraint. It is evidence for this commit, not reused evidence from pre-CR-002 run `31718156535`.

## Fixture-only CI lock

The committed workflow preserves NFR-003:

- Unit executes `python -m pytest tests/unit -q` and is explicitly labeled fixtures-only/no-live-provider.
- Integration executes `python -m pytest tests/integration -q` against an ephemeral Postgres service through `TEST_DATABASE_URL`.
- The workflow neither invokes `--provider nba-stats` nor configures a live sports endpoint.
- It does not provide or reference `secrets.API_SPORTS_KEY`.
- TEST-011 asserts the required jobs/DAG, absence of an API-Sports secret requirement, and image builds.
- Inspection of the CI test paths found fixture-provider use for ingest/load tests and no required NBA Stats host or HTTP client in the test suite. TEST-001's `test-key-not-real` is an isolated configuration/redaction sentinel, not a live credential or provider call.

Therefore the successful run does not make live NBA ingest a condition of green CI. The local live command remains a separate `--provider nba-stats` path.

## Requirements alignment

- **OPS-001:** satisfied for this publish-bar step. The remote run proves the declared lint ∥ unit → integration → image path is green on the reviewed mainline commit.
- **CON-005:** satisfied for this publish-bar step. The evidence is an actual GitHub Actions run of the repository workflow.
- **NFR-003:** held. Green CI remains fixture/offline-only and has no required live sports API call.
- **NFR-001:** remains **Partial**. A clean clone of current `main` following the README Compose fixture workflow is a separate owner attestation; this CI run does not establish it.

Owner-reported local live DR-001/ML-005 evidence (2,640 games, `ml005=True`, `logistic_regression-v1`, test log loss `0.623`) is out of scope for this gate. It does not change TEST-007 from synthetic and is not a PRD acceptance tick.

## Required traceability follow-up

Apply only these wording changes through the requirements-owning workflow; this review does not edit the primary traceability document.

1. Add ``Passing (remote)`` to the allowed Verification legend.
2. Set the OPS-001 Verification cell exactly to:

   `Passing (local); Passing (remote)`

3. Set the CON-005 Verification cell exactly to:

   `Passing (local); Passing (remote)`

4. Replace the stale Notes sentence `Remote GitHub Actions green is Deferred (workflow defined; TEST-011 asserts DAG locally).` with:

   `OPS-001 / CON-005 remote verification: GitHub Actions run [31753742525](https://github.com/RoeeHadar/AthletIQ/actions/runs/31753742525) succeeded for a push to main at head SHA 4a2f713ed4cf58966358c6dd12b3ea77813d905a; lint and unit succeeded in parallel, integration succeeded after both, and image succeeded after integration. TEST-011 remains Passing (local) for static DAG/policy verification; NFR-003 remains fixture/offline-only.`

Do not alter NFR-001, TEST-007, PRD acceptance, or the clean-machine attestation as part of this update.

## Non-blocking observation

All four jobs carry GitHub annotations that Node.js 20 actions are being forced onto Node.js 24 (`actions/checkout@v4`, and `actions/setup-python@v5` where used). The annotations are warnings, not job failures, and do not invalidate this gate.

## Validation

- [x] Run conclusion, event, branch, head SHA, jobs, and timing independently verified with `gh`
- [x] Exact workflow at the reviewed remote SHA inspected
- [x] TEST-011 and SRS OPS-001, CON-005, NFR-003, and NFR-001 boundaries checked
- [x] No local pytest result treated as remote CI evidence
- [x] No live-provider CI requirement introduced or recommended
- [x] NFR-001 remains Partial; no Compose e2e pytest requested
- [x] No PRD acceptance tick, CR-001 reopening, player ingest, GCP, or application-auth scope added
- [x] Only this review artifact authored
- [x] No Grill-Me invocation
