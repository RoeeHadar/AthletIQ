# Engineering lifecycle

## Must never miss

- Non-trivial implementation requires upstream docs **`Approved`** per permanent gates. **Before Gate 6 code:** Approved Design, **Implementation Plan**, and **Test Strategy** (prefer Test Plan) — Gate 7 *number* is verification closeout; Test Strategy must not stay Draft while coding (`docs/00-meta/gates.md` §22). Trivial fixes exempt. Enforced by `.cursor/rules/engineering-lifecycle.mdc`.
- **Gates 0–5 + Gate 7 Approved; Gate 6 — IMP-001…012 Done.** Post-IMP remainings closed **locally** (store/TEST detail: `integration-invariants`). **Remote CI green** on `RoeeHadar/AthletIQ` `main` — run [31718156535](https://github.com/RoeeHadar/AthletIQ/actions/runs/31718156535) (lint, unit, integration, image).
- Never invent product facts, requirements, users, metrics, or architectural decisions. Invoke **Grill-Me** (`.cursor/skills/grill-me/`) before `[OPEN QUESTION]` / `[ASSUMPTION]` / guessing — placeholders only after Grill-Me cannot resolve.
- **PRD vs Charter boundary:** PRD = product capabilities/users only; portfolio technical must-demonstrate constraints (Docker Compose, FastAPI, API-Sports, LR/XGBoost, GHA) live in **Charter** and ADRs — do not re-lock them as PRD product features.
- ADR files are owned exclusively by `architecture-decisions`; other skills flag `[DECISION PENDING: see ADR-XXX]` and never author ADRs. `engineering-review` flags drift only — never authors primary content, never calls Grill-Me.
- Approved Charter/PRD product intent ≠ Accepted architecture. Pipeline sketches remain **intent only** until ADRs exist — never walk through, implement, or cite as settled. If jump-to-architecture before SRS: draft **Proposed** ADRs only — Accept only after SRS exists and user approves.
- **No separate pending-approval folder** — all docs under `docs/`; approval = each file’s **`Status` header** (`Draft` → `In Review` → `Approved`). Index: `docs/README.md`; gate rules: `docs/00-meta/gates.md`.
- **Traceability columns are independent:** requirement, **implementation**, and **verification/test** status are separate — never set Implementation=`Implemented` solely because a test is Passing; update each column only from direct evidence for that column.
- **Docs honesty (2026-08-13):** Baseline managing-architect **7/10** → honesty pass + **CR-001** → dual review **9/10** (Grok + Lead GPT 5.6 Sol). Reports: `docs/00-meta/reviews/2026-08-13-grok-docs-rereview-2.md`, `2026-08-13-lead-sol-docs-review-2.md`. Approved docs **≠** MVP-complete or publish-bar satisfied.
- **Do not demand for docs/MVP closeout:** player ingest (CR-001 reserved tables), GCP, application auth, NBA **ML-005** attestation, or ticking PRD acceptance not yet true. **NFR-001** stays **Partial** until owner attestation of documented clean-clone Compose workflow. Remote CI (OPS-001/CON-005) is attested; do not re-open it as a blocker.
- **Publish-bar execution (owner-authorized 2026-08-13):** Proceed without re-approving Gates 0–7. **Per step:** QA crew runs documented tests → lead engineer approves → **lead manager (GPT 5.6 Sol)** reviews deliverables vs Approved docs + results before the next step. **Stop** for owner input, contradiction needing owner, unknown next steps, or **MVP ready for manual test** (then lead-manager critique/fix loop until approved). Live provider is unblocked (CR-002 / ADR-011). Do not invent missing infra/secrets. Do not add Compose e2e pytest to close NFR-001 (LEAD-001 / dual-review 9/10). **No `task-decomposer` skill in repo** — lead engineer decomposes manually. **QA crew:** `scripts/crews/qa/` (CrewAI-shaped YAML + deterministic `run_qa.py`) — engineering tooling only, not AthletIQ runtime / no crewai product dep.
- **Publish-bar Step 1 APPROVED (2026-08-13):** TEST-001…014 pytest green; every SRS Must has a TEST id. OPS-001/CON-005 remote CI is green. Review: `docs/00-meta/reviews/2026-08-13-lead-step1-qa.md`. **NFR-001** still not a clean GitHub clone. **DR-001 / ML-005** locally attested 2026-08-14 (see workspace bullet) — not yet on remote `main`.
- **2026-08-14** · `SOURCE: agent` · **Workspace:** Git + `origin` `https://github.com/RoeeHadar/AthletIQ.git`; Docker Desktop running; Compose `database`/`etl`/`api` up. Live `--provider nba-stats` ingest+train on Postgres: **2640** games (2023: 1319, 2024: 1321), 30 teams, `ml005=True`, pin `logistic_regression-v1`, test log loss 0.623. `/v1/health` + `/v1/model` after API restart. NFR-001 still not a clean GitHub clone. Live code not yet on remote `main` unless committed. No player ingest, GCP, or auth.

## Notes

- **2026-08-13** · `SOURCE: consolidate` · **WHY:** Squeezed prior consolidate trails. Process: 8 skills; bidirectional traceability; drift triple; CRs re-enter Requirements. Gate 6 was unblocked by Approved test strategy/plan v1.0.0 (user Approve over “await Approve”); ADR-010 Accepted; design/impl plan v1.0.1; lean test docs; `traceability.md` canonical req↔test; NFR-004 = no hard SLO via TEST-008; TEST-001…014 (Gate 5 §18 for 001–012; testing owns cases).
- **2026-08-13** · `SOURCE: sweep` · Docs review loop closed **9/10** (Grok + Lead Sol) after CR-001, independent traceability columns, LEAD-001 honest NFR-001/FR-012 labels; MVP/publish bar still open (PRD acceptance unchecked, NFR-001 Compose, live NBA/ML-005). Remote CI attested same day — see Must never miss.
- **2026-08-13** · `SOURCE: sweep` · Owner authorized publish-bar continuation; Step 1 gated (QA crew TEST-001…014 green, lead-manager APPROVE); per-step QA→lead engineer→lead manager process locked.
- **2026-08-13** · `SOURCE: agent` · Blockers cleared through Compose demo: git + `RoeeHadar/AthletIQ` + GHA green; owner enabled Docker; local README fixture pipeline + `/v1/health` + `/v1/model` ok (fixes not yet on remote `main`). Live DR-001/ML-005 blocked on provider access (`athletiq-product`).
